{
  "name": "tetris",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsup src/index.ts --format iife --clean --minify",
		"zip": "npm run build && rm -rf release && mkdir release && cp index.html release/ && cp -r dist release/ && zip -r Game.zip release"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
